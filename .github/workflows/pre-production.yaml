name: 预生产模式

on:
  push:
    branches:
      - main

jobs:
  build:
    name: 构建
    runs-on: self-hosted
    steps:
      - name: ⬇️ Checkout
        uses: actions/checkout@v4

      - name: 📦 安装依赖
        run: pnpm dev
